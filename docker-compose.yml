services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports: ["8080:5000"]
    depends_on: [db]
    environment:
      DATABASE_URL: postgresql://postgres:demo@db:5432/postgres
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: demo
    ports: ["5432:5432"]
